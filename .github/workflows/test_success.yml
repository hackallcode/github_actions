name: Test Success

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed
  workflow_dispatch:

jobs:
  test-success:
    runs-on: ubuntu-latest
    steps:
      -
        name: Sleep
        id: sleep
        run: |
          sleep 15
